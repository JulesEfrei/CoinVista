"use client";

import Button from "@atoms/Button";

export default function Home() {
  return (
    <>
      <h1>Dashboard</h1>
      <Button type="button" onClick={() => {}} disabled>
        Submit
      </Button>
    </>
  );
}
